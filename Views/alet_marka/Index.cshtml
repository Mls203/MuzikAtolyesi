@model IEnumerable<FinalProje.Models.alet_marka>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.markaAdi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Muzik.aletAdi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.markaAdi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Muzik.aletAdi)
        </td>
        <td>
            @Html.ActionLink("Düzenle", "Edit", new { id=item.markaId }) |
            @Html.ActionLink("Detay", "Details", new { id=item.markaId }) |
            @Html.ActionLink("Sil", "Delete", new { id=item.markaId })
        </td>
    </tr>
}

</table>
